Ensembling with Google Perch v2.0 is precisely how top competitors cross the $0.90+$ threshold while respecting Kaggle's 90-minute CPU constraint.Here is the technical reality of how heavy it is, how to optimize it for your hardware, and how to execute it without timing out.1. Will it be heavy and time-consuming?Training Time (Local Local Setup): VERY FAST. Because you have 64GB RAM, you can use a "Freeze and Cache" workflow. You run your entire dataset through the Perch model once, save the 1536-dimensional vectors to your disk as .npy files, and train lightweight downstream networks on top of them. A multi-layer perceptron (MLP) or simple Sound Event Detection (SED) head training on cached embeddings takes less than 5 minutes per fold on an RTX 3060.Inference Time (Kaggle CPU): MODERATE.The Perch v2.0 TensorFlow/ONNX model is highly optimized. Passing a 5-second audio chunk through Perch on a CPU takes roughly 15–20 milliseconds. For a standard 10-minute soundscape test run, Perch inference takes about 2–3 seconds. Running Perch is exceptionally efficient.2. Strategy 1: Ensembling Within Different Perch-Derived Heads (Recommended)Instead of training completely separate heavy architectures, you train different heads on top of the exact same frozen Perch backbone embeddings.                  ┌──► Strategy A Head (BCE + Label Smoothing) ──► Logits A
                  │
[Perch Embedding]─┼──► Strategy B Head (Custom Soft AUC Loss)  ──► Logits B
                  │
                  └──► Strategy C Head (Temporal SED / Attention) ─► Logits C
Why this is efficient: During Kaggle inference, you only extract the Perch embedding once per 5-second window. Then, you pass that single embedding through 3 or 4 ultra-lightweight linear heads.Token/Compute Footprint: Nearly zero extra CPU overhead during submission.3. Strategy 2: Ensembling Perch with your EfficientNet-B0Blending an image-based model (EfficientNet-B0 looking at Mel-spectrograms) with a feature-based model (Perch looking at raw waveforms) provides exceptional architectural diversity, which directly maximizes Macro ROC-AUC.The Constraint: Running both an audio feature extractor (Perch) and a Mel-spectrogram generator + Convolutional Neural Network (EfficientNet) on a Kaggle CPU can push close to the 90-minute wall if your post-processing is unoptimized.The Solution: Use Power-Rank Ensembling instead of simple averaging to ensure that the weaker model doesn't pull down the superior classifications of the stronger one.4. Step-by-Step Implementation via Claude CodeTo build a CPU-safe ensemble pipeline using your current repository layout, tell Claude Code to execute these two tasks sequentially:Task A: Create the Embedding Cache ScriptBash"Write a script `scripts/cache_perch_embeddings.py` that loads the competition audio files, extracts 1536-dimensional features using Google Perch v2.0, and saves them as compact .npy arrays to take advantage of our 64GB local RAM."
Task B: Update the Inference PipelineBash"Modify `kaggle_notebook/inference.py` to load both the frozen Perch model and our Phase 4 EfficientNet-B0 checkpoints. Ensure Perch computes features first, and apply rank-aware power scaling (power=0.6) when blending their probability distributions to prevent CPU execution timeouts."
Summary for your CLAUDE.mdEnsembling with Perch is not too heavy if you separate the feature-extraction pass from the classification heads. This single design adjustment gives you the feature diversity of a deep ensemble with the execution speed of a single lightweight network.
